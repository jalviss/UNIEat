$(() => {

    $("#login-btn").click(() => {
        var emailOrName = $("#emailOrName").val()
        var password = $("#password").val()

    if(emailOrName.split("@").length == 1){
       
    }
    else{
       
    }

    $("#login-form").submit();
    })

})

